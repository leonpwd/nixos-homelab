# Debug headers (visible in response headers)
add_header X-GeoIP-Country "$geoip2_data_country_code" always;
add_header X-GeoIP-Allowed "$allowed_country" always;

# Global GeoIP Blocking Rule: Drop non-allowed countries with 444
if ($allowed_country = 0) {
    return 444;
}
